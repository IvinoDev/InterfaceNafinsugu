import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.nafinsugu',
  appName: 'nafinsugu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
